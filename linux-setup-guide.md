To install on Linux:
- Download the proper install script based on your distro
- Open a terminal, and go to the directory the script downloaded to (most likely cd ~/Downloads/)
- Run the command chmod +x <distro>_nsb_install.sh
- Run the command ./<distro>_nsb_install.sh
- Follow any instructions that may appear
